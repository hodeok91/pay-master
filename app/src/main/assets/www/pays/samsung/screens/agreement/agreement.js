import {router} from "../../../../core/navigation/router.js"; import {screen,bottomButton} from "../common.js";
export function renderAgreement(){const s=screen("이용 약관");s.innerHTML+=`<div style="padding:20px 28px"><h1>카드 등록을 위한 약관에 동의해 주세요.</h1><div class="panel" style="margin-top:30px;padding:22px;font-size:22px">${["전체","Samsung Pay 서비스 이용약관 (필수)","개인정보 수집 및 이용 (필수)","전자금융거래 이용약관 (필수)"].map((x,i)=>`<label style="display:block;padding:18px 0;border-bottom:1px solid #444"><input type="checkbox" ${i===0?'id="all"':''}> ${x}</label>`).join("")}</div></div>`;const b=bottomButton(s,"계속",()=>router.go("samsung.fingerprint"));s.querySelector("#all").onchange=e=>s.querySelectorAll("input").forEach(x=>x.checked=e.target.checked);return s;}
