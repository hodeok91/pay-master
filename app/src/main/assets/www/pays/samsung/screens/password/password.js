import {router} from "../../../../core/navigation/router.js"; import {screen} from "../common.js"; import {load,save} from "../../../../core/storage/storage.js";
export function renderPassword(){return keypadScreen("결제 비밀번호 등록","터치하여 사용할 결제 비밀번호를 등록하세요.",digits=>{sessionStorage.setItem("newPayPw",digits);router.go("samsung.passwordConfirm")});}
export function keypadScreen(title,sub,onDone){const s=screen(title);s.innerHTML+=`<div class="center" style="padding-top:70px"><h1>${title}</h1><div class="muted">${sub}</div><div class="dots" id="dots">○ ○ ○ ○ ○ ○</div><div class="keypad">${[1,2,3,4,5,6,7,8,9,"⌫",0,"↻"].map(x=>`<button>${x}</button>`).join("")}</div></div>`;let d="";s.querySelectorAll(".keypad button").forEach(b=>b.onclick=()=>{const v=b.textContent;if(v==="⌫")d=d.slice(0,-1);else if(v==="↻")d="";else if(d.length<6)d+=v;s.querySelector("#dots").textContent=[0,1,2,3,4,5].map(i=>i<d.length?"●":"○").join(" ");if(d.length===6)setTimeout(()=>onDone(d),180)});return s;}
